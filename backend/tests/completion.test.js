const test=require('node:test'),assert=require('node:assert/strict'),{complete,checkedCompletion}=require('../services/completion');
test('refused, truncated and blank provider outputs are never successful drafts',()=>{for(const choice of [{message:{content:' '}},{message:{content:'partial'},finish_reason:'length'},{message:{content:'x',refusal:'no'}}])assert.throws(()=>checkedCompletion({data:{choices:[choice]}}));});
test('generation has a deadline and preserves source grounding instructions',async()=>{let seen;const http={post:async(...args)=>{seen=args;return{data:{choices:[{message:{content:'A complete draft.'},finish_reason:'stop'}]}}}};await complete({model:'fixture',messages:[{role:'system',content:'Draft text'},{role:'user',content:'source'}]}, {},http);assert.equal(seen[2].timeout,45000);assert.match(seen[1].messages[0].content,/Do not invent/);});
